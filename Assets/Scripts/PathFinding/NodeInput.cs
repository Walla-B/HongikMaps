using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NodeInput : MonoBehaviour
{
    private int inputcounter = 0;
    // Update is called once per frame
    void Update()
    {
        //psudocode
        /*
            if (input is in) {
                if (nodecounter < 2) {
                    getnode[nodecounter] = input node

                    nodecounter++;
                }
                if (nodecounter == 2) {
                    resultnode = Dijkstrasolve(input node 1, input node 2)
                    
                    Visualizepathresult (node)
                }
            }

        */
    }
}
